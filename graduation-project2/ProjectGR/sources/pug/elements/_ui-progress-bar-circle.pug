//- Circle progress bar
	@param {object} data
	@param {string} data.title
	@param {number} data.value
mixin progress-circle( data )
	//Circle progress bar
	.progress-circle&attributes( attributes )
		.progress-circle-block
			svg.progress-circle-bar( x='0', y='0', width='170', height='170', viewbox='0 0 170 170' )
				circle.progress-circle-bg( cx=85, cy=85, r=81 )
				circle.progress-circle-fg.clipped( cx=85, cy=85, r=81 )
			.progress-circle-content
				.progress-circle-counter= data.value
				p.progress-circle-title= data.title
