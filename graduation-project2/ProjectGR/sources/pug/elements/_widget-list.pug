//- Social List Widget
//- socials - an array of {link}
//- iclass - icon class
//- lclass - list class

mixin widget-list(socials, iclass, lclass)
  - var ilclass = iclass ? " " + iclass : " icon-xxs"
  - var lclass = lclass

  ul.list-inline(class=lclass)
    each link in socials
      li
        a(href=link.href class=link.icon + ilclass)